// Enable collection only after owner-scoped consent and withdrawal are supported.
// This pauses new training archives; it does not delete any existing user data.
export const TRAINING_COLLECTION_ENABLED = false;
